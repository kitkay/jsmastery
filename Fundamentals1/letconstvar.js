/**
 * let for variables that could change later.
 * const - used to variables that cannot changed.
 * var - works like let
 *
 * never use var as possible.
 * immutable means unchanging over time or unable to be chagnged.
 *  or immutable simply means it channot mutate.
 */

//Re-assigning or mutate the value
let age = 30;
age = 35;

console.log(age);