/**
 * Precendence operator
 */

let x, y; //Create variables without assignments.
x = y = 25 - 10; //Now assign values to our valriables.
console.log(x, y);

let curyear = 2022;
const ageMarvin = (curyear - 1986);
const ageKay = (curyear - 1988);
const averageAge = ((ageMarvin + ageKay) / 2);
//Averaging
console.log(averageAge);